# @package _global_

# to execute this experiment run:
# python run.py experiment=quick_test

defaults:
  - override /trainer: minimal.yaml # choose trainer from 'configs/trainer/'
  - override /model: qa_retriever.yaml
  - override /datamodule: fz_x_medqa.yaml
  - override /callbacks: # list callbacks using `- callback_i`
    - none # disable callbacks for faster training
  - override /logger: null # wandb.yaml

# all parameters below will be merged with parameters from default configurations set above
# this allows you to overwrite only specified parameters

seed: 42

trainer:
  min_epochs: 1
  max_epochs: 100
  gradient_clip_val: 0.5
  checkpoint_callback: False # disable checkpointing for faster training

model:
  lr: 2e-5

datamodule:
  train_batch_size: 2
  max_length: 100
  use_subset: True
  update_cache: True

#logger:
#  wandb:
#    group: quick_test
