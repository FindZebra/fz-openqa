# @package _global_

defaults:
  - override /model/head: colbert # todo colbert
  - override /datamodule/index_builder: colbert # todo colbert


# OpenQA builder parameters
builder:
  dataset_builder:
    n_query_tokens: 10 # todo: debug

# Index parameters
index_builder:
  faiss_args:
    factory: IVF100,PQ16x8
