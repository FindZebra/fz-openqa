_target_: fz_openqa.datamodules.corpus_dm.CorpusDataModule

input_dir: "${original_wdir}/examples"
cache_dir: ${sys.cache_dir} # cache_dir is specified in config.yaml
train_batch_size: ${datamodule.train_batch_size}
eval_batch_size: ${datamodule.eval_batch_size}
num_workers: ${datamodule.num_workers}
pin_memory: ${datamodule.pin_memory}
persistent_workers: ${datamodule.persistent_workers}
max_length: null
use_subset: False
num_proc: ${datamodule.num_proc}
passage_length: 100
passage_stride: 50
verbose: ${verbose}
