# @package _global_

# to execute this experiment run:
# poetry run fzqa experiment=reader_only

defaults:
  - override /trainer: minimal.yaml # choose trainer from 'configs/trainer/'
  - override /model: qa_retriever.yaml
  - override /datamodule: fz_x_medqa.yaml
  - override /callbacks: default.yaml # list callbacks using `- callback_i`
  - override /logger: wandb.yaml

# all parameters below will be merged with parameters from default configurations set above
# this allows you to overwrite only specified parameters

seed: 42

trainer:
  gpus: 1
  min_epochs: 1
  max_epochs: 100
  gradient_clip_val: 0.5
  # checkpoint_callback: False # disable checkpointing for faster training

model:
  lr: 2e-5

datamodule:
  train_batch_size: 4
  max_length: 512
  use_subset: False
  update_cache: True

logger:
  wandb:
    group: retriever-only # name your experiment here