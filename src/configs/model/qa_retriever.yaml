defaults:
  - evaluator: ir_gold_supervised.yaml

_target_: src.modeling.models.qa_retriever.QaRetriever

hidden_size: 32
bert_id: ${pretrained_model}
cache_dir: ${data_dir}
lr: 2e-5
weight_decay: 0
dropout: 0